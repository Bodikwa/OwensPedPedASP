@model OwensPedPed.DAL.Models.Provence

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Provence</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ProvenceName" class="control-label">Provence Name</label>
                <input asp-for="ProvenceName" class="form-control" />
                <span asp-validation-for="ProvenceName" class="text-danger">Provence Name</span>
            </div>

            <div class="form-group">
                <label asp-for="CountryId" class="control-label">Country Name</label>
                <select id=" CountryId" class="from-control" asp-for=" CountryId" asp-items="@ViewBag.countryOption">
                    <option value=" ">--select--</option>
                </select>
                <span asp-validation-for=" CountryId" class="text-danger">Country Name</span>
            </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
